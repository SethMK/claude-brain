---
type: lint-report
date: 2026-05-04
---

## Orphans (0)

0 — clean. Every wiki page has at least one incoming `[[wikilink]]` from another wiki page.

## Broken links (2)

Both are in `wiki/people/Andrej Karpathy.md` line 31, explicitly tagged `_(wanted)_`. Persistent from the 2026-04-26 ingest — not fixed by any subsequent lint cycle:

- `[[swyx]]` — no page in `wiki/people/` or elsewhere
- `[[LLM engineering]]` — no page in `wiki/concepts/` or elsewhere

## Stale stubs (0)

0 — clean. Two pages carry `status: stub`; neither is stale (threshold: last_updated older than 2026-04-20):

- `wiki/people/Dario Amodei.md` — last_updated: 2026-04-27 (7 days ago)
- `wiki/concepts/Second Brain.md` — last_updated: 2026-04-26 (8 days ago)

Both will cross the 14-day threshold within the next week if no ingest happens.

## Contradictions (0)

0 — clean. Spot-checked same-topic clusters (Claude Code + Skills + Hooks + Subagents; Karpathy LLM Wiki + Knowledge graph + Second Brain; Mike Krieger across Agent-native product, Cowork, MCP). No factual disagreements found.

One notable schema gap (not a contradiction): `wiki/people/Dario Amodei.md` has `sources: []` and all four factual claims explicitly marked `[unverified]`. The page self-flags this and lists four candidate sources. Not a contradiction — correctly quarantined — but the stub will rot.

## Gaps (0)

0 — clean. No `[[X]]` wikilink appears ≥ 3 times without a matching page. The two broken links (`swyx`, `LLM engineering`) each appear exactly once.

## Structural notes

- **INDEX.md**: exists at project root; covers all 22 pages including Simon Willison. Previous lint (2026-04-27 08:30) flagged it as missing — no longer an issue.
- **wiki/workflows/**: directory absent and no workflow pages exist. CLAUDE.md defines the type; absence is a content gap, not a schema violation.

## Next research (3)

1. **swyx (Shawn Wang)** — `[[swyx]]` has been a broken wanted link since the original April 26 ingest. swyx.io covers LLM engineering, agents, and AI product strategy — high overlap with this wiki's domain. A `/scan` for "swyx shawn wang AI agent 2026" should surface a clean ingest target.
2. **Dario Amodei — "The Adolescence of Technology"** (darioamodei.com, Jan 2026) — already listed as the top candidate source in the stub. One ingest verifies all four claims and lifts the page from stub to draft before the 14-day stale threshold.
3. **A workflow page** — no `wiki/workflows/` exists yet despite it being a defined page type. The most natural first candidate: the `/ingest` workflow itself (fetch → transcript → compile → log), which is documented in CLAUDE.md but not as a wiki page a non-dev could reference.

## Top 3 fixes

1. **Ingest a Dario Amodei source** — stub has `sources: []` and will go stale by ~2026-05-10. Candidate sources are pre-listed in the page; this is low-friction.
2. **Create `wiki/people/swyx.md`** — resolves the longest-standing broken link (since 2026-04-26). swyx is an active voice on exactly the topics this wiki covers (LLM engineering, agentic AI, Claude Code ecosystem).
3. **Create the first workflow page** — no workflow pages exist despite the type being defined. Documenting even one recurring workflow (e.g. "Research dossier in 30 min" or the ingest pipeline) would make the wiki actionable, not just encyclopedic.
